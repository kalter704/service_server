from django.conf.urls import url

from . import views

urlpatterns = [
	#url(r'^$', views.index, name='index'),
	#url(r'^addcategorie/', views.addCategorie, name='addCategorie'),
	#url(r'^adddish/', views.addDish, name='addDish'),
	
]
